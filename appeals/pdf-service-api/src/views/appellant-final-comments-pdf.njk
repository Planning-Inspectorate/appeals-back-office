<!DOCTYPE html>
<html lang="en" >
<head>
    {% include "pdf-head.njk" %}
</head>
<body >
  	 {% include "pdf-header.njk" %}

  <h1 class="govuk-heading-m">Appellant Final Comments</h1>

  <div class="site-address-header">
    <p><strong>Site address</strong>
    {% if appellantFinalCommentsData.appealSite %}
        {{ appellantFinalCommentsData.appealSite.addressLine1 | default('') }}{% if appellantFinalCommentsData.appealSite.addressLine2 %}, {{ appellantFinalCommentsData.appealSite.addressLine2 }}{% endif %}, {{ appellantFinalCommentsData.appealSite.postCode | default('') }}
    {% else %}
        <span class="not-available">Site address not available.</span>
    {% endif %}
    </p>
  </div>

  <p><strong>Appeal Reference</strong> {{ appellantFinalCommentsData.appealReference | default('N/A') }}</p>

  <h2 >Submitted Final Comments</h2>
  {% if appellantFinalCommentsData.commentsList and appellantFinalCommentsData.commentsList.length %}
    {% for comment_item in appellantFinalCommentsData.commentsList %}
      <div class="comment-block">
        <p class="comment-meta">
          <strong>Submitted by</strong> {{ comment_item.author | default('N/A') }}<br>
          <strong>Date Received</strong> {{ comment_item.created | date('d MMMM yyyy, HH:mm') if comment_item.created else 'N/A' }}<br>
          <strong>Status</strong> {{ comment_item.status | default('N/A') }}
        </p>
        <p class="govuk-body">{{ comment_item.originalRepresentation | nl2br | default('No comment text provided.') | safe }}</p>

        {% if comment_item.attachments and comment_item.attachments.length %}
          <h3>Supporting Documents</h3>
          <ul class="document-list">
            {% for doc in comment_item.attachments %}
              <li>{{ doc.name | default('Unnamed document') }}</li>
            {% endfor %}
          </ul>
        {% else %}
          <p class="not-available">No supporting documents submitted with this comment.</p>
        {% endif %}
      </div>
    {% endfor %}
  {% else %}
    <p class="not-available">No appellant final comments found or submitted for this appeal.</p>
  {% endif %}

</body>
</html>
