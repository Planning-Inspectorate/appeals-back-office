BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[NeighbouringSite] ADD [source] NVARCHAR(1000) NOT NULL CONSTRAINT [NeighbouringSite_source_df] DEFAULT 'back-office';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
