BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[ServiceUser] ADD [anonymised] BIT NOT NULL CONSTRAINT [ServiceUser_anonymised_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
