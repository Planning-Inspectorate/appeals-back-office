name: 'Custom CodeQL configuration'

queries:
  - uses: security-extended
  - uses: security-and-quality
  - uses: ./.github/codeql/queries/test-query.ql

query-filters:
  - exclude:
    problem.severity: warning
  - exclude:
    precision: low
  - include:
    tags: security
