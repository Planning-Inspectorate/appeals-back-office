{
  "name": "@pins/appeals",
  "version": "0.0.0",
  "type": "module",
  "main": "index.d.ts",
  "private": true,
  "engines": {
    "node": ">=22 < 23"
  },
  "scripts": {
    "lint:js": "npx eslint .",
    "lint:js:fix": "npx eslint . --fix",
    "test": "cross-env NODE_ENV=test NODE_OPTIONS=\"--experimental-vm-modules --expose-gc\" npx jest --logHeapUsage --ci --maxWorkers=50%",
    "test:cov": "cross-env NODE_ENV=test NODE_OPTIONS=\"--experimental-vm-modules --expose-gc\" npx jest --logHeapUsage --ci --coverage",
    "tscheck": "npx tsc -p jsconfig.json --maxNodeModuleJsDepth 0"
  },
  "dependencies": {
    "date-fns": "^2.28.0",
    "date-fns-tz": "^2.0.0",
    "node-cache": "^5.1.2",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "type-fest": "^4.37.0"
  }
}
